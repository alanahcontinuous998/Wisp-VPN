<UserControl x:Class="WispVpn.App.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="220">

            <Ellipse Width="104" Height="104" Stroke="{StaticResource AccentBrush}" StrokeThickness="3"/>

            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,24"
                       FontSize="14"/>

            <Border Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="15"
                    Padding="12,6"
                    Margin="0,0,0,16">
                <TextBlock Text="{Binding SelectedServer}"
                           Foreground="{StaticResource TextBrush}"
                           HorizontalAlignment="Center"/>
            </Border>

            <Button Content="Подключиться"
                    Command="{Binding ConnectCommand}"
                    Foreground="{StaticResource AccentGreenBrush}"
                    Background="Transparent"
                    BorderBrush="{StaticResource AccentGreenBrush}"
                    Padding="16,8"/>
        </StackPanel>
    </Grid>
</UserControl>
